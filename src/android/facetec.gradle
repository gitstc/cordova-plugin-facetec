repositories {    
    jcenter()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    implementation files('libs/facetec-sdk.aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}